import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'PsicoStacks - Verify hiring credentials in seconds',
  description: 'AI makes results understandable; Bitcoin makes them provable and revocable. Share via one-time QR. Employers pay a small fee to verify.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
